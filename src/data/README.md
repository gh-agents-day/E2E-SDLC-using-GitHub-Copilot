/**
 * This directory contains JSON flat-file data stores for development
 * When USE_DATABASE=true in .env, data is persisted in a real database instead
 * 
 * Files:
 * - users.json: User accounts (id, email, password, role, etc.)
 * - tasks.json: Task records
 * - task-dependencies.json: Task dependency relationships
 * - status-history.json: Audit trail of status changes
 */
